# Small-Cafe-Management-System
A simple Python-based restaurant management system for a café. The program displays a menu, allows customers to select items, checks item availability, and calculates the total bill. Built using Python dictionaries, conditional statements, and user input to simulate a basic command-line food ordering system.
